# ── SQLite (por defecto, no requiere nada más) ──
# DB_PATH=./server/mundialito.db

# ── PostgreSQL (descomentá si querés usar Postgres) ──
# DATABASE_URL=postgresql://usuario:contraseña@localhost:5432/mundialito
# DB_SSL=true   # solo si tu hosting requiere SSL (ej. Render, Railway)

# Puerto del servidor
PORT=3001
